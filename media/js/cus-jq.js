var base_url = location.protocol + "//" + location.hostname + "/greenandwhite/";

$(document).ready(function(){
	/*
	$('.sbox-button').click(function(){
		var params = {
			type: 'GET',
			url: base_url + 'page/async_search_page',
			dataType: 'json',
			data: {
				searchitem: $('.sbox-text').val()
			},
			success: function(response){
				if(response.error){
					alert(response.message);
				} else {
					window.location.href = base_url + 'page/search_page_result/' + $('.sbox-text').val();
				}
			}
		};
		jQuery.ajax(params);
	});
	*/
});